{
  "name": "agent-kv-cache-slidev",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build slides.md --out dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check-deck.mjs"
  },
  "devDependencies": {
    "@slidev/cli": "52.19.1",
    "@slidev/theme-default": "0.25.0"
  }
}
